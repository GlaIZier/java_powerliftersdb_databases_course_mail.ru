version: "2"

services:

  postgres:
    image: postgres:9.6
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d

  tomcat:
    image: glaizier/powerlifters
    depends_on:
      - postgres
    ports:
      - "8888:8080"